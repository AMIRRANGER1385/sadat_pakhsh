# راهنمای عملیات Production

## زمان‌بندی ضروری

- هر ۵ دقیقه: `cron-expire.php` برای بررسی پرداخت، آزادسازی موجودی سفارش رهاشده و حذف Upload بدون استفاده پس از ۲۴ ساعت.
- هر ۱۰ دقیقه: `cron-health.php`. خروجی غیرصفر باید توسط ایمیل Cron یا سرویس مانیتورینگ هشدار داده شود.
- روزانه: `deploy/backup-shop.sh` برای Database، Uploads و کلید خصوصی 2FA.
- هفتگی: Full Backup حساب هاست و اجرای `deploy/restore-drill.sh` روی یک دیتابیس موقت و جدا.

Backup باید خارج از `public_html` و با دسترسی 0700 ذخیره شود. نگهداری نسخه روی همان هاست کافی نیست؛ متغیر `NAYLEX_RCLONE_REMOTE` باید به فضای خارج از سرور تنظیم شود. فایل `$HOME/.naylex-backup.cnf` باید 0600 و شامل کاربر محدود MySQL باشد. کلید `storage/two-factor.key` محرمانه است و نسخه خارج از سرور آن باید رمزنگاری شود.

## مانیتورینگ و لاگ

- `storage/logs/php-error.log`: خطاهای PHP بدون نمایش Stack Trace به مشتری.
- `storage/logs/events-YYYY-MM.jsonl`: خطاهای 5xx، درگاه، اجرای Cron و Health Check.
- `storage/expiry-cron.log`: نتیجه هر اجرای سفارش‌های Pending.
- Health Check اتصال Database، فضای آزاد دیسک، دسترسی Storage و تازه‌بودن Cron را کنترل می‌کند.

فایل‌های Log نباید در `public_html` باشند. برای 5xx و Disk باید هشدار خارجی cPanel/uptime نیز تنظیم شود؛ کد داخل همان سرور به‌تنهایی قطعی سرور را تشخیص نمی‌دهد.

## پرداخت و Refund

Mock Test جای تراکنش واقعی را نمی‌گیرد. پیش از فروش عمومی در مقصد، یک پرداخت کم‌مبلغ واقعی، لغو در درگاه، Callback تکراری، Timeout، پرداخت دیرهنگام، Pending منقضی و تطبیق مبلغ/Reference با پنل زرین‌پال ثبت شود.

SDK رسمی زرین‌پال Refund را از GraphQL و با Access Token انجام می‌دهد. این پروژه فقط Merchant ID دارد و بدون Access Token، شناسه Session معتبر و تست واقعی نباید عملیات مالی Refund را حدس بزند. تا دریافت این دسترسی، استرداد باید در پنل زرین‌پال انجام و نتیجه در سفارش ثبت شود. افزودن دکمه‌ای که موفقیت استرداد را بدون پاسخ قطعی API اعلام کند ممنوع است.

## Restore Drill

اسکریپت Restore، checksum را کنترل می‌کند، Dump را فقط در دیتابیسی با پیشوند `naylex_restore_drill_` بازیابی می‌کند، وجود جدول‌های اصلی را می‌سنجد و دیتابیس موقت را در پایان حذف می‌کند. نتیجه موفق باید تاریخ‌دار نگهداری شود؛ وجود فایل `backup.sql` بدون این آزمون کافی نیست.
