# ورود گروهی محصولات خرده و عمده

محصولات در فایل‌های PHP ذخیره نمی‌شوند. اطلاعات محصول در جدول `ns_products` دیتابیس MySQL و نوع فروش و اتصال خرده به عمده در جدول `ns_product_sales` ذخیره می‌شود. تصاویر آپلودشده نیز در پوشهٔ خصوصی `naylex-app/storage/uploads` قرار می‌گیرند و با مسیر `/media?name=...` نمایش داده می‌شوند. برای تغییر محصول، از پنل مدیریت یا ورود گروهی استفاده کنید و دیتابیس یا فایل‌های برنامه را دستی ویرایش نکنید.

## روش ورود با اکسل

1. وارد پنل مدیریت شوید و برگهٔ «ورود گروهی اکسل» را باز کنید.
2. قالب `products.xlsx` را دانلود کنید. نسخهٔ CSV UTF-8 نیز موجود است.
3. ردیف نمونه را با کالای واقعی جایگزین کنید. هر ردیف یک کالا است و سایت از آن یک محصول خرده و یک محصول عمده می‌سازد.
4. فایل را آپلود و پیش‌نمایش را بررسی کنید. در این مرحله هنوز چیزی ذخیره نمی‌شود.
5. دکمهٔ تأیید نهایی را بزنید تا تمام ردیف‌ها در یک تراکنش ذخیره شوند. اگر یک ردیف خطا داشته باشد، هیچ ردیفی ذخیره نمی‌شود.

دو شناسهٔ `retail_slug` و `wholesale_slug` باید انگلیسی، ثابت و متفاوت باشند. حالت «فقط افزودن» شناسهٔ تکراری را رد می‌کند. حالت «افزودن یا به‌روزرسانی» محصول موجود را با همان شناسه به‌روزرسانی می‌کند و محصول تکراری نمی‌سازد.

فیلد `threshold` تعداد شروع پیشنهاد عمده است. مثلاً مقدار ۲۵ با واحد «کیسه» یعنی وقتی مشتری در محصول خرده تعداد ۲۵ یا بیشتر را ثبت کند، کارت محصول عمدهٔ متصل کنار کنترل خرید نمایش داده می‌شود. این کارت عکس، نام، قیمت عمده و دکمهٔ انتخاب محصول عمده را دارد. سبد مشتری بدون انتخاب او خودکار جایگزین نمی‌شود.

قیمت و موجودی خرده و عمده مستقل‌اند. قالب فعلی واحد فروش مشترک دارد. ستون تصویر اختیاری است: در به‌روزرسانی اگر خالی باشد عکس قبلی حفظ می‌شود؛ محصول جدید با تصویر نمونه ساخته می‌شود و بعداً می‌توانید عکس واقعی را از فرم ویرایش همان محصول آپلود کنید.

حداکثر هر فایل ۵۰۰ کالا و ۳ مگابایت است. فایل نباید فرمول داشته باشد و نام یا ترتیب ستون‌ها نباید تغییر کند.
