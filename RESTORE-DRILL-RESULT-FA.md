# نتیجه Restore Drill محلی — ۱۴۰۵/۰۶/۲۵

- منبع: دیتابیس تست جداگانه `naylex_local` روی MariaDB محلی.
- مقصد: دیتابیس موقت با پیشوند `naylex_restore_drill_`؛ پس از آزمون حذف شد.
- Checksum فایل Dump: `AC280729357483C759D578ADDE6AE0F05B61DE10FAE555146D43EECD56135357` (SHA-256).
- پنج جدول حیاتی `ns_products`, `ns_users`, `ns_orders`, `ns_order_items`, `ns_settings` پس از بازیابی موجود بودند.
- تعداد بازیابی‌شده در زمان آزمون: ۱۴ محصول و ۰ سفارش.
- نتیجه: Dump، Import و کنترل ساختار موفق بود و دیتابیس اصلی تست تغییر نکرد.

این نتیجه فقط سلامت فرایند محلی را ثابت می‌کند. پس از راه‌اندازی Backup واقعی cPanel، Restore Drill باید با همان فایل Production در یک دیتابیس موقت هاست نیز اجرا و تاریخ نتیجه ثبت شود.
