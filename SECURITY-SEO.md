# گزارش اصلاحات نایلکس سادات

نتیجه بررسی این نسخه: ۱۲ تست داخلی، ۴۳ آزمون HTTP روی دیتابیس جداگانه، build تولید موفق و npm audit با صفر آسیب‌پذیری شناخته‌شده. تست‌های HTTP شامل تغییر رمز، ابطال نشست قدیمی، ذخیره اطلاعات شرکت، آپلود تصویر، ثبت محصول با تصویر آپلودشده و ذخیره سابقه ورود هستند. startup مخصوص cPanel محلی بررسی شده؛ اجرای واقعی Passenger هاست هنوز تأیید نشده است.

## مدیریت محتوا

- ورود: `/admin`؛ سپس «محصولات ← محصول جدید». نام، شناسه انگلیسی آدرس، توضیحات، تصویر، دسته‌بندی، قیمت‌ها، واحد، موجودی و حداقل عمده را وارد کنید.
- تصویر: علاوه بر آپلود مستقیم، می‌توانید فایل تصویر را در `public/products` قرار دهید و مسیر `/products/نام-انگلیسی.webp` را در فرم ثبت کنید. آپلود مستقیم JPEG/PNG/WebP تا ۵ مگابایت اکنون در فرم محصول فعال است؛ فایل بازکدگذاری و فراداده آن حذف می‌شود. WebP/JPEG/PNG با اندازه مناسب محصول توصیه می‌شود. SVG فقط برای تصاویر مورد اعتماد خودتان است.
- «دسته‌بندی‌ها»: افزودن و ویرایش دسته‌بندی.
- «اطلاعات شرکت»: نام رسمی، درباره ما، تلفن، ایمیل، آدرس، ساعات پاسخ‌گویی و کد پستی. خروجی فرم در درباره ما، تماس با ما و پایین سایت استفاده می‌شود. نام برند سایت «نایلکس سادات» است؛ نام رسمی شرکت می‌تواند متفاوت باشد.
- «امنیت حساب»: تغییر رمز فعلی و بستن تمام نشست‌های قبلی. نام کاربری اولیه `admin` است. رمز نصب اول در `.env` قرار دارد؛ پس از تغییر رمز از پنل، رمز دیتابیس ملاک است.

## SEO

- نام برند و عنوان‌ها اصلاح شدند؛ عنوان و توضیح محصول از اطلاعات واقعی آن تولید می‌شوند.
- canonical صفحات عمومی و محصولات؛ جستجو و فیلترهای محصولات noindex با امکان دنبال کردن لینک کالاها.
- صفحات درباره ما، تماس و پرسش‌ها قابل ایندکس شدند؛ حساب، سبد، تسویه، مدیریت، ورود و پیگیری noindex هستند.
- robots اجازه خواندن صفحات noindex را می‌دهد تا موتور جستجو دستور را ببیند؛ robots وسیله محافظت امنیتی نیست.
- sitemap محصولات فعال و صفحات عمومی را شامل می‌شود.
- Product/Offer با قیمت ریالی (تومان × ۱۰)، موجودی، URL و فروشنده؛ BreadcrumbList و Organization بر اساس داده‌های واقعی. امتیاز، نظر، سیاست مرجوعی یا نشانی ساختگی اضافه نشده است.
- JSON-LD در سرور تولید و `<` escape می‌شود تا توضیحات محصول نتواند تگ script را ببندد.
- Open Graph، اطلاعات اشتراک‌گذاری، favicon و لینک عبور به محتوای اصلی اضافه شدند.

برای انتشار: `APP_URL` را به دامنه واقعی HTTPS تغییر دهید، build جدید بگیرید، اطلاعات شرکت و توضیحات اختصاصی و تصاویر واقعی کالاها را تکمیل کنید. سایت محلی در گوگل قابل ثبت نیست. پس از انتشار، مالکیت دامنه را در Search Console تأیید و `/sitemap.xml` را ثبت کنید. Rich Results Test را روی URL محصولات اجرا کنید. رتبه یا نمایش rich result تضمین‌پذیر نیست و به محتوای واقعی و وضعیت دامنه نیز وابسته است.

منابع: [راهنمای داده‌های ساختاریافته فروشگاهی گوگل](https://developers.google.com/search/docs/specialty/ecommerce/include-structured-data-relevant-to-ecommerce)، [ساختار URL فروشگاه](https://developers.google.com/search/docs/specialty/ecommerce/designing-a-url-structure-for-ecommerce-sites).

## امنیت

- صفر آسیب‌پذیری شناخته‌شده در npm audit در زمان بررسی؛ دو وابستگی غیرمستقیم Prisma با override به نسخه اصلاح‌شده ارتقا یافتند. Prisma generate، ساخت پروژه و اتصال SQLite بعد از ارتقا تست شدند.
- نشست تصادفی با توکن هش‌شده در دیتابیس، HttpOnly، SameSite و Secure در تولید؛ گردش نشست در ورود و تغییر رمز؛ ابطال نشست در خروج.
- سقف بایت رمز bcrypt کنترل می‌شود؛ محاسبه هش برای نام کاربری ناموجود هم انجام می‌شود تا اختلاف زمانی کاهش یابد.
- بررسی Origin برای نوشتن، مجوز مدیر در سرور، اعتبارسنجی Zod، سقف واقعی ۳۲ کیلوبایت بدنه و رد JSON نامعتبر.
- محدودسازی اتمی درخواست با پنجره زمانی در دیتابیس، محدودیت سراسری هر مسیر و محدودیت حساب/تلفن؛ مسیر ناشناخته قبل از ساخت رکورد محدودسازی رد می‌شود. هدر IP ارسالی کاربر مبنای اعتماد نیست.
- CSP با nonce تصادفی؛ عدم unsafe-eval در تولید؛ منع iframe و object، محدودسازی منبع تصویر و اتصال، HSTS، منع sniff و سیاست عدم ارسال referrer.
- صفحات حساس و API فاقد کش عمومی‌اند.
- قیمت، تخفیف عمده، ارسال و موجودی در سرور محاسبه می‌شوند. کلید یکتای سفارش و هش محتوای آن از ثبت دوباره در تکرار یک درخواست جلوگیری می‌کند.
- پاسخ زرین‌پال از نظر کد، authority و ref_id اعتبارسنجی می‌شود؛ تغییر پارامتر payment در URL دیگر سبد را پاک نمی‌کند یا پرداخت تأییدشده نمایش نمی‌دهد. پاک شدن سبد به نتیجه پیگیری تأییدشده در سرور وابسته است.
- تغییر وضعیت سفارش شرطی است؛ درخواست هم‌زمان نمی‌تواند وضعیت قبلی را بازنویسی یا موجودی را دوباره آزاد کند.
- خطاهای داخلی و شبکه به کاربر افشا نمی‌شوند.

## حدود بررسی و الزامات استقرار

این اصلاحات تضمین نبود همه آسیب‌پذیری‌ها نیستند. بررسی نفوذ بیرونی، بار بالا، DNS/TLS و درگاه واقعی در محیط فعلی انجام نشده‌اند. درگاه به مرچنت نیاز دارد؛ تست پاسخ‌های ساختگی جای پرداخت سرتاسری Sandbox را نمی‌گیرد.

سفارش دارای authority که پرداختش رها شده، همچنان موجودی را رزرو نگه می‌دارد. آزادسازی کورکورانه خطر فروش بیش از موجودی یا گم‌شدن پرداخت دیرهنگام دارد؛ تطبیق دوره‌ای با سرویس درگاه و روند لغو/استرداد عملیاتی هنوز لازم است. درخواست‌های سفارش محدود شده‌اند، اما برای مقابله با حمله توزیع‌شده، محدودسازی لبه بر اساس IP معتبر در reverse proxy و کنترل ترافیک لازم است. سیاست فعلی سراسری در حمله شدید ممکن است مشتریان عادی را هم موقتاً محدود کند.

SQLite را روی دیسک دائمی و یک نمونه سرور نگه دارید؛ پشتیبان محرمانه است و نباید در public یا Git قرار گیرد. ارتقای این نوبت فقط ستون و ایندکس افزوده و تعداد رکوردها و integrity_check بررسی شده است. بررسی احراز هویت و تغییر محتوا روی کپی جداگانه دیتابیس انجام شد.

منبع رمز: [OWASP Password Storage](https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html).


آپلود فقط برای مدیر با کنترل Origin، محدودیت اندازه واقعی بدنه، امضای فایل، ابعاد حداکثر ۲۰ میلیون پیکسل، رد SVG/انیمیشن، نام UUID و ذخیره خارج از public انجام می‌شود. فایل‌های آپلودی عمومی‌اند و حذف خودکار فایل استفاده‌نشده فعلاً انجام نمی‌شود؛ مصرف دیسک را کنترل کنید. سوابق احراز هویت فقط در پنل مدیر نمایش داده می‌شوند؛ نشانی IP جمع‌آوری نمی‌شود.
