# گزارش بررسی SEO، امنیت و سرعت — ۱۴ سپتامبر ۲۰۲۶

## نتیجه سایت آنلاین

در بررسی عمومی، صفحه اصلی، /products، /products/، robots.txt و sitemap.xml پاسخ ۲۰۰ داشتند. صفحه اصلی آنلاین هنوز عنوان و شعار نسخه قدیمی را نمایش می‌دهد؛ پس وجود اصلاحات در Git به معنی انتشار آن‌ها نیست. robots.txt آدرس نقشه سایت را اعلام می‌کند. دسترسی به Google Search Console و وضعیت ایندکس از این بررسی قابل تأیید نیست.

## تغییرات و امکانات بررسی‌شده

- در نسخه پروژه، عنوان و توضیح صفحات، یک H1 اصلی، دسته‌بندی‌های دارای آدرس مستقل، canonical، اطلاعات ساختاریافته محصول و مسیر راهنما، نقشه سایت دارای دسته‌بندی و تصویر، و noindex برای فیلترها وجود دارند. ۲۴ آدرس نقشه سایت در محیط محلی بررسی شدند.
- در این مرحله آدرس‌های عمومی دارای اسلش پایانی با 301 به نسخه اصلی هدایت می‌شوند؛ پارامتر جستجو حفظ می‌شود.
- آدرس CSS و JavaScript با هش محتوای فایل نسخه‌گذاری شد تا پس از انتشار، مرورگر فایل جدید بگیرد. Apache برای CSS، JavaScript و فونت کش یک‌هفته‌ای و برای متن CSS/JS/SVG فشرده‌سازی دارد؛ فعال بودن ماژول‌های هاست لازم است. HTML حاوی حساب، سبد و CSRF همچنان private, no-store است.
- خواندن کالاهای سبد به صورت گروهی انجام می‌شود؛ برای ۱۰۰ کالا یک پرس‌وجوی محصول به جای ۱۰۰ پرس‌وجو لازم است. سبد در سمت سرور حداکثر ۱۰۰ نوع محصول می‌پذیرد.
- تصویر اشتراک‌گذاری از نشان سازمان در داده ساختاریافته جدا شد؛ نشان سازمان اکنون فایل آیکون سایت است.

## آزمون‌ها

پس از روشن کردن PHP و MySQL جداگانه محلی، این بررسی‌ها موفق بودند:

- ۱۶ آزمون منطق PHP/MySQL و ۱۲ سناریوی رزرو موجودی و پرداخت با پاسخ شبیه‌سازی‌شده درگاه.
- ورود با نام یکتا، رفتار نام تکراری و حفظ ورود با موبایل.
- ۲۴ صفحه نقشه سایت: پاسخ HTTP، H1، canonical، توضیحات، schema و لینک دسته‌بندی؛ noindex صفحات فیلتر و 404.
- مرورگر موبایل و دسکتاپ: رفتن به دسته‌بندی و محصول، افزودن و کم کردن سبد، ماندگاری تعداد، عدم سرریز افقی و نبود خطای JavaScript.
- رد CSRF مفقود، Origin خارجی، آپلود و عملیات مدیر توسط مهمان، تعداد نامعتبر و مسیر غیرمجاز تصویر؛ بررسی escape ورودی جستجو و هدرهای امنیتی.
- بررسی نحو ۱۹ فایل PHP.

دستور آزمون جدید: `python scripts/php-audit-regression.py`؛ فقط با سرور محلی localhost:8085 اجرا می‌شود. تست Apache، اندازه‌گیری Core Web Vitals واقعی و پرداخت واقعی در این گزارش انجام نشده‌اند. این گزارش تضمین نبود همه آسیب‌پذیری‌ها نیست.

## موارد باز و کارهای لازم

1. حداقل رمز فعلی مشتری طبق درخواست قبلی چهار کاراکتر است و ضعیف محسوب می‌شود. سؤال افزایش حداقل رمز مطرح شده و تا پاسخ شما تغییر نکرده است. بازیابی پیامکی هم همچنان غیرفعال است.
2. محصولات نمونه، عکس‌های placeholder، مشخصات ناقص و نشانی شرکت باید با اطلاعات واقعی جایگزین شوند. شهر، نشانی، اندازه، جنس، وزن، تعداد در بسته و شرایط ارسال را فقط مطابق واقعیت وارد کنید. ادعای تولیدکننده و قیمت کارخانه براساس تأیید خود شما در متن پروژه آمده است.
3. نتیجه URL Inspection صفحه اصلی در Search Console را دریافت کنید. HTML صفحه، نتیجه تأیید مالکیت یا ایندکس نیست. ثبت نقشه و درخواست ایندکس باید در حساب دارای دسترسی انجام شود؛ در این مرحله انجام نشده است.
4. پس از انتشار، PageSpeed Insights را برای صفحه اصلی و یک محصول روی موبایل اجرا کنید. بهبود کد به معنی امتیاز اندازه‌گیری‌شده یا تضمین سرعت هاست نیست.
5. تعداد بالای محصولات و سفارش‌ها در آینده نیازمند صفحه‌بندی پنل و کاتالوگ است. تغییر slug محصول فعلاً آدرس قدیمی را حفظ نمی‌کند؛ آدرس محصولات منتشرشده را بدون تعریف هدایت مناسب عوض نکنید.

## انتشار از Git

ابتدا از فایل‌ها و دیتابیس هاست پشتیبان بگیرید. تغییرات کد را commit و به شاخه متصل به cPanel push کنید. در **Git Version Control → Manage → Pull or Deploy** ابتدا **Update from Remote** و سپس **Deploy HEAD Commit** را اجرا کنید. صرف push به GitHub، انتشار خودکار را تضمین نمی‌کند. راهنمای دقیق در DEPLOY-GIT-FA.md است.

فایل‌های این مرحله: core.php، actions.php، pages.php، views.php در naylex-app و index.php و .htaccess در public_html. بسته کامل نیز با scripts/package-php.py ساخته می‌شود. config.php و storage هاست را حفظ کنید؛ نصب مجدد لازم نیست. مقدار app_url هاست باید https://sadatpakhsh.ir باشد.

پس از انتشار، /products/ باید به /products هدایت شود و در کد صفحه آدرس shop.css?v= و shop.js?v= دیده شود. عنوان اصلی و نقشه سایت جدید را کنترل کنید و ورود و سبد را یک بار آزمایش کنید. تغییرات این مرحله توسط من به هاست ارسال نشده‌اند.

گوگل رتبه اول یا حتی ایندکس را تضمین نمی‌کند. محتوای مفید، محصولات واقعی، اعتبار کسب‌وکار و رفع موانع فنی در کنار هم اهمیت دارند. منابع: [راهنمای SEO گوگل](https://developers.google.com/search/docs/fundamentals/seo-starter-guide)، [امنیت احراز هویت OWASP](https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html)، [کش HTTP](https://web.dev/articles/http-cache).
