# نقشه سایت و معرفی به گوگل

## انتشار بسته جدید و بررسی ایندکس

بسته dist/naylex-sadat-php82.zip از کل کد فعلی PHP و فایل‌های عمومی ساخته می‌شود و شامل لوگو، پیشنهاد جستجو، پنل مقاله، راهنماهای خرید و اصلاحات SEO است. config.php و storage شخصی داخل آن نیستند. ابتدا پشتیبان بگیرید؛ naylex-app را کنار public_html و محتویات public_html بسته را داخل ریشه وب دامنه جایگزین کنید. config.php و storage موجود را حفظ کنید؛ نصب مجدد لازم نیست.

در بررسی مستقیم آنلاین این مرحله، robots.txt و sitemap.xml هر دو پاسخ ۲۰۰ دادند. نقشه آنلاین هنوز ۱۳ آدرس نسخه قدیمی داشت؛ وجود این دو مسیر به معنی انتشار کد جدید یا ایندکس‌شدن صفحات نیست. نسخه جدید `/sitemap.xml` یک Sitemap Index پویا است و صفحات عمومی، دسته‌ها، محصولات فعال، راهنماهای خرید و مقاله‌های منتشرشده را در نقشه‌های جدا معرفی می‌کند. تعداد دقیق به دیتابیس هاست بستگی دارد.

صفحه‌های ورود، حساب، سبد، تسویه، پیگیری، فرم همکاری و جستجوی داخلی noindex هستند و داخل نقشه نیستند. robots.txt عمداً آن‌ها را مسدود نمی‌کند تا گوگل بتواند noindex را بخواند. robots ابزار حفاظت از حساب نیست؛ حفاظت مدیر با احراز هویت انجام می‌شود. منابع CSS، JavaScript، تصاویر و آیکون باید برای خزنده قابل دریافت باشند.

برای اتصال گوگل:

1. در Search Console یک Domain property برای sadatpakhsh.ir بسازید.
2. رکورد TXT اختصاصی نمایش‌داده‌شده را در DNS فعال دامنه ثبت کنید. اگر نام‌سرورها متعلق به هاست هستند، معمولاً Zone Editor هاست محل رکورد است؛ پنل ثبت دامنه لزوماً DNS را میزبانی نمی‌کند.
3. پس از انتشار رکورد، Verify را بزنید. مقدار TXT را از حساب خودتان بگیرید؛ در پروژه تولید نمی‌شود.
4. در Sitemaps مقدار sitemap.xml را ارسال کنید و وضعیت Success یا خطا را ببینید.
5. در URL Inspection صفحه اصلی، products، about، contact، دسته‌ها و محصولات واقعی را بررسی کنید؛ برای چند صفحه مهم Request Indexing بزنید، بقیه از نقشه و لینک‌های داخلی کشف می‌شوند.
6. در گزارش Pages علت‌های ایندکس‌نشدن را بخوانید و در Performance نمایش و کلیک کلمات عمومی را دنبال کنید. نتیجه site: فهرست کامل و قطعی ایندکس نیست.

تأیید مالکیت، ارسال نقشه و Request Indexing از این محیط انجام نشده‌اند؛ نیازمند ورود به حساب Google مالک سایت هستند. برای ادامه بررسی، نتیجه URL Inspection یا خروجی Pages کافی است؛ رمز حساب را نفرستید. ایندکس و رتبه اول تضمین نمی‌شوند.

اگر فایل فیزیکی sitemap.xml یا robots.txt از نسخه قبلی در ریشه وب وجود دارد، قبل از کنارگذاشتن از آن پشتیبان بگیرید؛ این فایل می‌تواند مسیر پویا را بپوشاند. پس از آپلود، نقشه باید شامل /guides و /wholesale-buying و دسته‌ها باشد و هیچ URL لوکال نداشته باشد.

آدرس اصلی: `https://sadatpakhsh.ir/sitemap.xml`

### تکمیل سایت‌مپ برای فروش خرده و عمده

صفحهٔ `/wholesale` نیز در نقشهٔ صفحات عمومی قرار دارد. تمام محصولات فعال خرده و عمده، هر کدام با آدرس مستقل صفحهٔ محصول، در نقشهٔ محصولات معرفی می‌شوند. صفحات ورود، مدیریت، سبد، پرداخت، پنل قدیمی عمده، جست‌وجو و فیلترها وارد نقشه نمی‌شوند.

هر زیرنقشه حداکثر ۵۰۰۰ آدرس دارد و پیش از رسیدن به ۴۵ میلیون بایت تقسیم می‌شود. قسمت‌های بعدی مانند `/sitemaps/products-2.xml` خودکار به ایندکس اصلی اضافه می‌شوند. تاریخ نامعتبر یا آینده حذف می‌شود و تاریخ ساختگی «امروز» تولید نمی‌شود. برای فایل ایندکس، تاریخ آخرین محصول به‌اشتباه به‌عنوان زمان تغییر کل فایل استفاده نمی‌شود. تصویرها با `image:loc` معرفی می‌شوند؛ تگ قدیمی `image:title` حذف شده است.

پس از انتشار بسته روی هاست:

1. `https://sadatpakhsh.ir/sitemap.xml` را باز کنید؛ باید XML شامل `sitemapindex` باشد.
2. بررسی کنید دامنهٔ تمام لینک‌ها `https://sadatpakhsh.ir` باشد، نه `localhost`.
3. در Google Search Console، پراپرتی همین دامنه را انتخاب کنید و به **Sitemaps → Add a new sitemap** بروید.
4. مقدار `sitemap.xml` را وارد کنید؛ اگر کادر آدرس کامل می‌خواهد، `https://sadatpakhsh.ir/sitemap.xml` را وارد و **Submit** را بزنید.
5. وضعیت خواندن نقشه را بررسی کنید. اگر فایل قدیمی `sitemap.xml` یا `robots.txt` در public_html وجود دارد، مطابق توضیح پایین از آن پشتیبان بگیرید و مانع مسیر پویا را برطرف کنید.

نیازی به ارسال دوباره پس از هر تغییر محصول نیست؛ نقشه از دیتابیس ساخته می‌شود. ثبت نقشه تضمین ایندکس یا رتبه نیست. راهنمای رسمی: https://developers.google.com/search/docs/crawling-indexing/sitemaps/build-sitemap

برای کنترل کامل خروجی، `python scripts/php-sitemap-test.py https://sadatpakhsh.ir` را بعد از استقرار اجرا کنید. این آزمون تمام زیرنقشه‌ها، پاسخ ۲۰۰ هر صفحه، canonical منطبق، نبود noindex، تصاویر و robots.txt را بررسی می‌کند. آزمون محلی بدون آرگومان اجرا می‌شود. نتیجهٔ محلی معادل تأیید انتشار روی دامنه نیست.

این خروجی به‌صورت پویا ساخته می‌شود و به فایل دستی یا Cron نیاز ندارد. `/sitemap.xml` ایندکس اصلی است و در صورت داشتن محتوای قابل انتشار، این نقشه‌ها را معرفی می‌کند:

- `/sitemaps/pages.xml`: صفحات عمومی و حقوقی
- `/sitemaps/products.xml`: محصولات فعال همراه تصویر و `lastmod`
- `/sitemaps/categories.xml`: دسته‌های دارای محصول
- `/sitemaps/guides.xml`: راهنماهای خرید
- `/sitemaps/articles.xml`: فقط مقاله‌های فعال و منتشرشده

محصول ناموجود تا وقتی فعال باشد باقی می‌ماند و محصول غیرفعال حذف می‌شود. برای تصاویر محصول و مقاله از افزونه استاندارد Image Sitemap استفاده شده است. `lastmod` فقط از تاریخ واقعی ذخیره‌شده محتوا ساخته می‌شود و برای صفحات عمومی تاریخ ساختگی درج نمی‌شود. در Search Console فقط آدرس اصلی `sitemap.xml` را ثبت کنید؛ لازم نیست پنج زیرنقشه را جداگانه ارسال کنید.

## انتشار

بسته جدید را روی هاست جایگزین کنید و config.php و storage را حفظ کنید؛ نصب مجدد یا تغییر ساختار دیتابیس لازم نیست. فایل جدید `naylex-app/sitemap.php` نیز باید منتقل شود. مقدار app_url روی هاست باید `https://sadatpakhsh.ir` باشد. اگر خطای MySQL 1045 دارید، قبل از ثبت نقشه آن را رفع کنید؛ sitemap به دیتابیس نیاز دارد.

1. `/sitemap.xml` را روی دامنه باز کنید؛ باید عنصر `sitemapindex` و آدرس دامنه واقعی دیده شود، نه localhost، صفحه خطا یا صفحه ورود. سپس حداقل `/sitemaps/products.xml` و `/sitemaps/guides.xml` را نیز باز کنید.
2. `/robots.txt` باید آدرس همان sitemap را اعلام کند. اگر فایل فیزیکی قدیمی sitemap.xml یا robots.txt در public_html دارید، پس از پشتیبان‌گیری آن را کنار بگذارید تا مسیر پویا اجرا شود.
3. بازکردن `http://sadatpakhsh.ir/` باید با پاسخ ۳۰۱ به `https://sadatpakhsh.ir/` منتقل شود؛ نسخه HTTP نباید یک صفحه ۲۰۰ جداگانه ارائه کند.
4. در Google Search Console مالکیت دامنه را تأیید کنید. برای Domain property رکورد TXT ارائه‌شده گوگل را در DNS فعال دامنه اضافه کنید؛ مقدار را دقیقاً از حساب خودتان دریافت کنید.
5. در بخش Sitemaps آدرس بالا را ثبت کنید و نتیجه پردازش و خطاها را بررسی کنید.
6. در URL Inspection صفحه اصلی، `/plastic-products`، `/wholesale-buying` و چند محصول واقعی را بررسی کنید. کشف، خزیدن و ایندکس‌شدن مراحل متفاوت‌اند؛ ارسال نقشه تضمین ایندکس یا زمان مشخص نیست.

## برای نتیجه بهتر در جستجو

نام و توضیح اختصاصی محصولات، عکس واقعی و واضح، قیمت و موجودی صحیح، اطلاعات تماس کامل و تجربه خرید سالم را تکمیل کنید. صفحات نمونه و متن آزمایشی را پیش از انتشار اصلاح کنید. لینک‌های داخلی محصولات باید در موبایل نیز قابل استفاده باشند. وضعیت صفحات در Search Console را دنبال کنید؛ sitemap به‌تنهایی رتبه اول ایجاد نمی‌کند.

گوگل priority و changefreq را نادیده می‌گیرد؛ lastmod باید با تغییر واقعی محتوا سازگار باشد. در این پروژه ویرایش محصول و تغییر موجودی زمان محصول را به‌روز می‌کند؛ صرف بازشدن sitemap تاریخ را تغییر نمی‌دهد.

منبع: [راهنمای رسمی ساخت و ارسال sitemap گوگل](https://developers.google.com/search/docs/crawling-indexing/sitemaps/build-sitemap).
