# نقشه سایت و معرفی به گوگل

آدرس اصلی: `https://sadatpakhsh.ir/sitemap.xml`

این خروجی XML به‌صورت پویا از محصولات فعال MySQL ساخته می‌شود؛ نیازی به ساخت فایل دستی یا اجرای Cron برای sitemap نیست. پنج صفحه اصلی و آدرس اصلی محصولات در آن هستند. محصول ناموجود تا وقتی فعال باشد باقی می‌ماند؛ محصول غیرفعال حذف می‌شود. آدرس تصاویر با افزونه استاندارد image و تاریخ آخرین تغییر ذخیره‌شده محصولات با lastmod ارائه می‌شود. برای صفحات عمومی که تاریخ معتبر ندارند تاریخ ساختگی درج نمی‌شود. برای تعداد فعلی محصولات، یک sitemap کافی است.

## انتشار

بسته جدید را روی هاست جایگزین کنید و config.php و storage را حفظ کنید؛ نصب مجدد یا تغییر ساختار دیتابیس لازم نیست. فایل جدید `naylex-app/sitemap.php` نیز باید منتقل شود. مقدار app_url روی هاست باید `https://sadatpakhsh.ir` باشد. اگر خطای MySQL 1045 دارید، قبل از ثبت نقشه آن را رفع کنید؛ sitemap به دیتابیس نیاز دارد.

1. `/sitemap.xml` را روی دامنه باز کنید؛ باید XML و آدرس دامنه واقعی دیده شود، نه localhost، صفحه خطا یا صفحه ورود.
2. `/robots.txt` باید آدرس همان sitemap را اعلام کند. اگر فایل فیزیکی قدیمی sitemap.xml یا robots.txt در public_html دارید، پس از پشتیبان‌گیری آن را کنار بگذارید تا مسیر پویا اجرا شود.
3. در Google Search Console مالکیت دامنه را تأیید کنید. برای Domain property رکورد TXT ارائه‌شده گوگل را در DNS فعال دامنه اضافه کنید؛ مقدار را دقیقاً از حساب خودتان دریافت کنید.
4. در بخش Sitemaps آدرس بالا را ثبت کنید و نتیجه پردازش و خطاها را بررسی کنید.
5. در URL Inspection صفحه اصلی و چند محصول واقعی را بررسی کنید. کشف، خزیدن و ایندکس‌شدن مراحل متفاوت‌اند؛ ارسال نقشه تضمین ایندکس یا زمان مشخص نیست.

## برای نتیجه بهتر در جستجو

نام و توضیح اختصاصی محصولات، عکس واقعی و واضح، قیمت و موجودی صحیح، اطلاعات تماس کامل و تجربه خرید سالم را تکمیل کنید. صفحات نمونه و متن آزمایشی را پیش از انتشار اصلاح کنید. لینک‌های داخلی محصولات باید در موبایل نیز قابل استفاده باشند. وضعیت صفحات در Search Console را دنبال کنید؛ sitemap به‌تنهایی رتبه اول ایجاد نمی‌کند.

گوگل priority و changefreq را نادیده می‌گیرد؛ lastmod باید با تغییر واقعی محتوا سازگار باشد. در این پروژه ویرایش محصول و تغییر موجودی زمان محصول را به‌روز می‌کند؛ صرف بازشدن sitemap تاریخ را تغییر نمی‌دهد.

منبع: [راهنمای رسمی ساخت و ارسال sitemap گوگل](https://developers.google.com/search/docs/crawling-indexing/sitemaps/build-sitemap).
