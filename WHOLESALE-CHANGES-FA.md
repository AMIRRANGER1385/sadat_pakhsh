# فروش عمده، FAQ و جست‌وجو

این تغییرات برای نسخه PHP مستقرشونده توسط `.cpanel.yml` هستند.

## مدیریت و قیمت

- `/admin?tab=products&edit=0`: پیش‌فرض محصول جدید ۵۰ کیلوگرم است. آستانه و قیمت عمده هر محصول جداگانه قابل ویرایش است. با تغییر واحد به بسته، مقدار آستانه تعداد بسته خواهد بود؛ وزن بسته بدون اطلاعات واقعی به کیلو تبدیل نمی‌شود.
- مقدارهای صریح محصولات موجود حفظ می‌شوند. برای تغییر آن‌ها، آستانه همان محصول را در مدیریت ویرایش کنید.
- `/wholesale`: صفحه عمومی و قابل ایندکس همکاری، با لینک درخواست ثبت‌نام و امکان درخواست از حساب موجود.
- `/wholesale-panel`: مخصوص حساب تأییدشده؛ قیمت کمتر از خرده، صرفه‌جویی هر واحد و افزودن به سبد. نرخ همان قیمت عمده ثبت‌شده محصول است؛ تخفیف سومی اختراع نشده است.
- تأیید از بخش مشتریان مدیر انجام می‌شود. قیمت سبد و تسویه از وضعیت حساب در سرور محاسبه می‌شود.
- سفارش ویژه بالای ۱۰۰ کیلوگرم از تخفیف تعدادی هر کالا تفکیک شده و نیازمند استعلام است.

## محتوای عمومی

متن بالای صفحه اصلی موضوع فروش و مخاطب را مستقیم بیان می‌کند. FAQ از یک منبع مشترک برای متن قابل مشاهده و JSON-LD ساخته می‌شود. پاسخ‌های ارسال، خرید حضوری، فاکتور، مرجوعی و تولید سفارشی طبق درخواست صاحب سایت خالی‌اند و تا تکمیل، در صفحه و Schema منتشر نمی‌شوند.

خوشه‌های تجاری در صفحه اصلی، محصولات، دسته‌ها و `/wholesale` پوشش داده می‌شوند. خوشه‌های آموزشی جدید:

- `/guides/nylon-vs-nylex`
- `/guides/nylex-count-per-kilo`
- `/guides/nylex-thickness-guide`

این صفحات از راهنماها، دسته‌ها و sitemap لینک دارند. Schema به معنی تضمین نمایش ویژه در گوگل نیست.

## جست‌وجو

پیشنهاد تصویر، نام، دسته، قیمت و واحد با فوکوس کادر و هنگام تایپ نمایش داده می‌شود. یکسان‌سازی حروف عربی/فارسی، تطبیق نام و دسته و تحمل یک حرف اشتباه برای کلمه‌های چهارحرفی و بیشتر فعال است. جست‌وجوی ارسال‌شده نیز همین تطبیق را استفاده می‌کند. تطبیق تقریبی به ۵۰۰ محصول محدود است؛ برای کاتالوگ بزرگ‌تر موتور جست‌وجوی ایندکس‌شده لازم می‌شود. آمار عبارت‌های شخصی جست‌وجو ذخیره نمی‌شود.

## بررسی

تست‌های PHP/MySQL، ممیزی امنیتی محلی، تست مرورگری موبایل (درخواست و تأیید همکاری، دسترسی پنل، پیشنهاد جست‌وجو و تطابق FAQ Schema) و بررسی صفحات sitemap اجرا شدند. داده حساب آزمایشی پس از تست حذف می‌شود.

درخواست PageSpeed موبایل برای دامنه با 403 رد شد؛ LCP، INP و CLS واقعی اندازه‌گیری نشده‌اند. پس از انتشار، آزمون موبایل PageSpeed/Lighthouse و داده میدانی باید جداگانه بررسی شوند.

تغییرات محلی‌اند؛ انتشار روی هاست انجام نشده است.
