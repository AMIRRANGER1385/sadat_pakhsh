'use client';
import { useState } from 'react';
import { api } from './shop';
export function PasswordForm(){const [message,setMessage]=useState(''),[busy,setBusy]=useState(false);return <form className="panel form-stack" onSubmit={async e=>{e.preventDefault();setBusy(true);setMessage('');const form=e.currentTarget;try{await api('auth/password',Object.fromEntries(new FormData(form)));setMessage('رمز تغییر کرد و نشست‌های قبلی بسته شدند.');form.reset()}catch(e){setMessage((e as Error).message)}finally{setBusy(false)}}}><h2>تغییر رمز عبور</h2><label>رمز فعلی<input name="currentPassword" type="password" autoComplete="current-password" required maxLength={72}/></label><label>رمز جدید<input name="newPassword" type="password" autoComplete="new-password" required minLength={8} maxLength={72}/></label><small>حداقل ۸ کاراکتر و حداکثر ۷۲ بایت. برای عبارت طولانی فارسی، محدودیت بایت را در نظر بگیرید.</small>{message&&<p role="status">{message}</p>}<button className="btn" disabled={busy}>{busy?'در حال ذخیره...':'تغییر رمز و بستن نشست‌های قبلی'}</button></form>}
